/**
 * Created by ABHISHEK on 02-05-2019.
 */
({
    /**
     * Controller method to handle the data loading on initial stage
     * @param c
     * @param e
     * @param h
     */
    loadInitialData : function (c,e,h) {
        h.loadInitialData_Helper(c,e,h);
    }
})