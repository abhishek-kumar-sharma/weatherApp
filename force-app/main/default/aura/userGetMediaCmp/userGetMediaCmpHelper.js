/**
 * Created by ABHISHEK on 26-04-2019.
 */
({
    handleInit_Helper: function (c, e, h) {
        /*let video;
        let constraints = {audio: true, video: {width: 1280, height: 720}};
        console.log('navigator ==>',c.navigator);
        console.log('get user media ==>',c.navigator.getUserMedia(constraints));
        c.navigator.mediaDevices.getUserMedia(constraints)
            .then(function (mediaStream) {
                console.log('promise resolved ',mediaStream);
                video = document.querySelector('video');
                video.srcObject = mediaStream;
                video.onloadedmetadata = function (e) {
                    video.play();
                };
            })
            .catch(function (err) {
                console.log(err.name + ": " + err.message);
            }); // always check for errors at the end.
*/
    }
})